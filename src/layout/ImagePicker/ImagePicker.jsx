import React from 'react';
import FilePicker from '../../atom/FilePicker/FilePicker';

const ImagePicker = () => {
  return (
    <div>
      <FilePicker />
    </div>
  );
};

export default ImagePicker;
